#include "error.ch"

PROCEDURE Main(arg1, arg2)

  //? "Hello world", ROUND(2.335)
 
  //? "CTOD()", CTOD()
 
  nRet := BROWSE(0, 0)
 
  ?  Seconds(2)


  // __ERRRT_SBASE( EG_ARG, 1094, NIL, "Main", 25 )
  RETURN
